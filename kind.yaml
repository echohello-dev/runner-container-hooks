kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
- role: control-plane
  extraMounts:
  - hostPath: ${PWD}/packages/k8s/tests
    containerPath: ${PWD}/packages/k8s/tests
    readOnly: false
    selinuxRelabel: false
    propagation: None
